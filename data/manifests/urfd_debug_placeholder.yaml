# Placeholder URFD debug manifest.
#
# This file ships in the repo so:
#   - the manifest loader has something to chew on for tests,
#   - the validator's pipeline runs cleanly against the placeholder,
#   - the structure is documented by example.
#
# The REAL manifest is auto-generated by `data/build_urfd_manifest.py`
# after `data/stage_urfd.py:stage_urfd_from_kaggle()` has placed URFD
# on Drive. Once the real manifest exists, it will OVERWRITE this file.
# The two have the same shape; the real one has more rows.
#
# Schema version: 1.1 (matches `data/manifests/__init__.py:CURRENT_SCHEMA_VERSION`).

schema_version: "1.1"

clips:

  # ---------- DEBUG tier (URFD-only — falls) ----------
  - clip_id: urfd-debug-fall-01-cam0
    dataset: urfd
    role: debug
    label: fall
    source_path: datasets/urfd/fall-01-cam0
    notes: "sequence=01; camera=cam0; frame-folder (PNGs); PLACEHOLDER"

  - clip_id: urfd-debug-fall-01-cam1
    dataset: urfd
    role: debug
    label: fall
    source_path: datasets/urfd/fall-01-cam1
    notes: "sequence=01; camera=cam1; frame-folder (PNGs); PLACEHOLDER"

  # ---------- DEBUG tier (URFD-only — non-falls / ADL) ----------
  - clip_id: urfd-debug-adl-01-cam0
    dataset: urfd
    role: debug
    label: no_fall
    source_path: datasets/urfd/adl-01-cam0
    notes: "sequence=01; camera=cam0; frame-folder (PNGs); PLACEHOLDER"

  - clip_id: urfd-debug-adl-01-cam1
    dataset: urfd
    role: debug
    label: no_fall
    source_path: datasets/urfd/adl-01-cam1
    notes: "sequence=01; camera=cam1; frame-folder (PNGs); PLACEHOLDER"